-- Inisialisasi layanan Roblox
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local Player = Players.LocalPlayer

-- Membuat GUI di ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = Player:WaitForChild("PlayerGui")
ScreenGui.Name = "WalvyHub"

-- Frame utama GUI
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 400, 0, 300)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainFrame.BorderSizePixel = 5
MainFrame.Parent = ScreenGui

-- Judul GUI
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Text = "Walvy Hub"
TitleLabel.Size = UDim2.new(1, 0, 0, 50)
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 24
TitleLabel.TextAlign = Enum.TextXAlignment.Center
TitleLabel.Parent = MainFrame

-- Tombol AutoFarm
local AutoFarmButton = Instance.new("TextButton")
AutoFarmButton.Size = UDim2.new(0, 180, 0, 50)
AutoFarmButton.Position = UDim2.new(0.5, -90, 0, 70)
AutoFarmButton.Text = "Aktifkan AutoFarm"
AutoFarmButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
AutoFarmButton.Font = Enum.Font.Gotham
AutoFarmButton.TextSize = 20
AutoFarmButton.TextColor3 = Color3.fromRGB(255, 255, 255)
AutoFarmButton.Parent = MainFrame

-- Tombol Teleport
local TeleportButton = Instance.new("TextButton")
TeleportButton.Size = UDim2.new(0, 180, 0, 50)
TeleportButton.Position = UDim2.new(0.5, -90, 0, 130)
TeleportButton.Text = "Aktifkan Teleport"
TeleportButton.BackgroundColor3 = Color3.fromRGB(0, 255, 255)
TeleportButton.Font = Enum.Font.Gotham
TeleportButton.TextSize = 20
TeleportButton.TextColor3 = Color3.fromRGB(255, 255, 255)
TeleportButton.Parent = MainFrame

-- Tombol ESP
local ESPButton = Instance.new("TextButton")
ESPButton.Size = UDim2.new(0, 180, 0, 50)
ESPButton.Position = UDim2.new(0.5, -90, 0, 190)
ESPButton.Text = "Aktifkan ESP"
ESPButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
ESPButton.Font = Enum.Font.Gotham
ESPButton.TextSize = 20
ESPButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ESPButton.Parent = MainFrame

-- Tombol WalkSpeed
local WalkSpeedButton = Instance.new("TextButton")
WalkSpeedButton.Size = UDim2.new(0, 180, 0, 50)
WalkSpeedButton.Position = UDim2.new(0.5, -90, 0, 250)
WalkSpeedButton.Text = "Ubah WalkSpeed"
WalkSpeedButton.BackgroundColor3 = Color3.fromRGB(255, 165, 0)
WalkSpeedButton.Font = Enum.Font.Gotham
WalkSpeedButton.TextSize = 20
WalkSpeedButton.TextColor3 = Color3.fromRGB(255, 255, 255)
WalkSpeedButton.Parent = MainFrame

-- Tombol JumpPower
local JumpPowerButton = Instance.new("TextButton")
JumpPowerButton.Size = UDim2.new(0, 180, 0, 50)
JumpPowerButton.Position = UDim2.new(0.5, -90, 0, 310)
JumpPowerButton.Text = "Ubah JumpPower"
JumpPowerButton.BackgroundColor3 = Color3.fromRGB(255, 255, 0)
JumpPowerButton.Font = Enum.Font.Gotham
JumpPowerButton.TextSize = 20
JumpPowerButton.TextColor3 = Color3.fromRGB(255, 255, 255)
JumpPowerButton.Parent = MainFrame

-- Fungsi untuk mengaktifkan AutoFarm
local function activateAutoFarm()
    print("AutoFarm diaktifkan!")
    -- [Implementasi AutoFarm] – Gantikan dengan logika AutoFarm yang sesuai
end

-- Fungsi untuk mengaktifkan Teleport
local function activateTeleport()
    print("Teleportasi ke lokasi farm...")
    local targetPosition = Vector3.new(100, 10, 200) 
    Player.Character:SetPrimaryPartCFrame(CFrame.new(targetPosition))
end

-- Fungsi untuk mengaktifkan ESP
local function activateESP()
    print("ESP diaktifkan...")
    -- [Implementasi ESP] – Gantikan dengan logika ESP yang sesuai
end

-- Fungsi untuk mengubah WalkSpeed
local function changeWalkSpeed()
    print("WalkSpeed diubah!")
    Player.Character.Humanoid.WalkSpeed = 100
end

-- Fungsi untuk mengubah JumpPower
local function changeJumpPower()
    print("JumpPower diubah!")
    Player.Character.Humanoid.JumpPower = 50
end

-- Menghubungkan tombol dengan fungsi masing-masing
AutoFarmButton.MouseButton1Click:Connect(activateAutoFarm)
TeleportButton.MouseButton1Click:Connect(activateTeleport)
ESPButton.MouseButton1Click:Connect(activateESP)
WalkSpeedButton.MouseButton1Click:Connect(changeWalkSpeed)
JumpPowerButton.MouseButton1Click:Connect(changeJumpPower)
